"""
toolkit — the fixed action space: the only analysis the agent can perform.
functions.py holds the implementations; registry.py declares which of them are callable and what
arguments they accept. Nothing outside this package is reachable by the agent.
"""
