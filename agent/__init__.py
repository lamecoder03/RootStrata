"""
agent — the hand-written planning loop over Groq, and nothing else.
llm.py adapts the OpenAI-compatible endpoint, planner_loop.py is the loop itself, trace.py writes
the run out for a human to read, run.py is the CLI. No framework, so the control flow is readable.
"""
